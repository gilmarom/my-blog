import { GoogleMap.DirectiveDirective } from './google-map.directive.directive';

describe('GoogleMap.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleMap.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
